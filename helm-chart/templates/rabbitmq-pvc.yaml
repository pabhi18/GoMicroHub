apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "helm-chart.fullname" . }}-rabbitmq
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ include "helm-chart.fullname" . }}-storage
  resources:
    requests:
      storage: {{ .Values.rabbitmq.persistence.size }} 