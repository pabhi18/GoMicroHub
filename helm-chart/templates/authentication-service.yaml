apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-chart.fullname" . }}-auth
  labels:
    {{- include "helm-chart.labels" . | nindent 4 }}
    app.kubernetes.io/component: auth
spec:
  type: {{ .Values.service.authenticationService.type }}
  ports:
    - port: {{ .Values.service.authenticationService.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-chart.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: auth